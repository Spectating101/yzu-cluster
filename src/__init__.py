"""
Compatibility package used by the FinSight-style API code.

Several modules under `Sharpe-Renaissance/api/` import from `src.*`.
This project snapshot does not use a `src/` layout, so we provide a small
shim package that re-exports the intended modules.
"""

