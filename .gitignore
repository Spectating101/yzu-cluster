node_modules/
dist/
.tmp-pw/
test-results/
playwright-report/
artifacts/
.env
.env.local

# Screenshots are committed for ChatGPT / GitHub visual review
!docs/screenshots-review/
!docs/screenshots-review/**
!research-drive-screenshots.zip

# Per-tree role marker. Must stay untracked: these checkouts are worktrees of
# one repository, so a committed file would carry identical content into every
# tree and could not distinguish them — which is the whole point of it.
.desk-role
